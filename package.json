{
  "name": "playwright-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest --config=./jest.config-custom.js --runInBand",
    "test-js": "jest",
    "test-debug": "cross-env PWDEBUG=1 jest --config=./jest.config-debug.js --runInBand",
    "codegen": "playwright-cli codegen"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "expect-playwright": "^0.3.3",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0",
    "jest-playwright-preset": "^1.5.1",
    "lodash.merge": "^4.6.2",
    "playwright": "^1.9.2",
    "playwright-cli": "^0.180.0"
  },
  "dependencies": {},
  "directories": {
    "test": "tests"
  },
  "description": "",
  "jest-junit": {
    "outputDirectory": "reports"
  }
}